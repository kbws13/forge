# {{ spec.name }} Agent Service

基于 [kbws-forge-runtime](https://pypi.org/project/kbws-forge-runtime/) 的分层 FastAPI Agent 服务，由 [forge](https://github.com/kbws13/forge) 的 `{{ spec.template }}` 模板生成。

## 架构设计

整个项目沿**两条正交的轴**组织：

1. **业务聚合轴（`agents/`）**：一个 agent = 一个目录，prompt / tools / agent 定义全部内聚。
2. **技术分层轴（`app/`）**：跨 agent 的基础设施与交互层（配置 / 异常 / 鉴权 / 日志 / 路由 / 编排）。

```
{{ spec.name }}/
├── agents/                          # ★ 业务聚合层：一个子目录 = 一个 agent
│   ├── {{ spec.module_name }}/      #   主 agent（agent_id = {{ spec.module_name }}）
│   │   ├── agent.py                 #   Agent 定义（导出 `agent` 对象，SDK 自动发现）
│   │   ├── prompts.py               #   代码化 prompt 组件（人设/指令块）
│   │   ├── tools.py                 #   本 agent 的工具
│   │   └── (middleware 示例见 agent.py)
│   └── extract/                     #   结构化输出示例（output_schema）
│       ├── agent.py                 #   pydantic schema -> Agent(output_schema=...)
│       └── prompts.py
├── app/                             # 技术分层
│   ├── main.py                      #   create_app() 工厂 + lifespan：load_agents 自动注册
│   ├── core/                        #   基础设施（config/enums/exceptions/handlers/response/security/logging/utils）
│   ├── middleware/request_id.py     #   请求追踪 ID
│   ├── api/v1/                      #   controller 层（agents/sessions/chat/health）
│   ├── schemas/                     #   dto/vo
│   ├── services/chat_service.py     #   业务编排 + SDK 错误码转换
│   └── providers/llm.py             #   model 工厂
├── tests/                           # unit / api / integration(真实 provider)
├── scripts/run.sh                   # 启动脚本
└── .env.example                     # 配置模板
```

## 配置

全部配置集中在 `app/core/config.py`（pydantic-settings），加载顺序：`.env` → `.env.{APP_ENV}` → 真实环境变量。

| 配置项 | 默认 | 说明 |
| --- | --- | --- |
| `APP_ENV` | `dev` | 环境：`dev` / `prod` / `test` |
| `DEBUG` | `false` | 是否开启 /docs 调试文档 |
| `API_KEY` | 非空占位 | 服务鉴权 key（`X-API-Key` 或 Bearer 头）。**为空时鉴权静默关闭**，生产必须设置强随机值 |
| `LOG_DIR` | `logs` | 日志目录 |
| `LOG_LEVEL` | `INFO` | 日志级别 |
| `AGENTS_DIR` | `agents` | 业务聚合层根目录 |
| `RUN_TIMEOUT_SECONDS` | 不限制 | 单次 Agent 运行超时 |
| `RUN_MAX_MODEL_CALLS` | 不限制 | 单次运行最大模型调用数 |
| `RUN_MAX_TOOL_CALLS` | 不限制 | 单次运行最大工具调用数 |
| `RUN_MAX_TOTAL_TOKENS` | 不限制 | 单次运行最大 token 数；模型不返回 usage 时失败关闭 |
| `RUN_MAX_COST_USD` | 不限制 | 单次运行最大费用；模型不返回 cost 时失败关闭 |
| `RUN_MAX_CONCURRENCY` | 不限制 | 单次运行内模型/工具动作并发上限 |
| `RUN_MODEL_RETRIES` | `0` | 模型调用重试次数 |
| `RUN_TOOL_RETRIES` | `0` | 声明为 retryable 的工具重试次数 |
| `RUN_RETRYABLE_TOOLS` | `[]` | 允许自动重试的工具名 JSON 数组 |
| `RUN_RETRY_BACKOFF_SECONDS` | `0` | 重试退避秒数 |
| `DEEPSEEK_API_KEY` | 必填（填在 `.env`） | 模型供应商 key；integration 测试也读取它 |
| `DEEPSEEK_BASE_URL` | `https://api.deepseek.com` | 模型 API 地址 |
| `DEEPSEEK_MODEL` | `deepseek-chat` | 模型名 |

## 运行

```bash
uv sync
cp .env.example .env        # 填入 DEEPSEEK_API_KEY，并修改 API_KEY
./scripts/run.sh            # 默认 dev，带热重载
APP_ENV=prod ./scripts/run.sh
```

另开终端启动本地 Trace UI：

```bash
forge trace --api-url http://127.0.0.1:8000/api/v1
```

UI 只监听 `127.0.0.1`，加载后自动连接服务（连接设置保存在浏览器 localStorage），运行历史展示的是服务端记录的全量 trace（`/api/v1/traces`），每几秒自动刷新，其它客户端产生的 run 也会自动出现。

接口：`GET /api/v1/health`（无鉴权）、`GET /api/v1/agents`、`POST /api/v1/sessions`、`POST /api/v1/chat`、`POST /api/v1/chat_stream`（SSE）、`GET /api/v1/traces`（运行历史列表）、`GET /api/v1/traces/{run_id}`（完整执行流），统一响应 `{code, info, data}`，鉴权头 `X-API-Key: <key>` 或 `Authorization: Bearer <key>`。

`POST /api/v1/chat` 会返回 `duration_ms`、`model_calls`、`tool_calls` 和 `usage`。`POST /api/v1/chat_stream` 会按 `sequence` 输出完整运行轨迹，包括 `model_started/model_finished/model_failed`、`tool_started/tool_finished/tool_failed` 和运行终态事件。

每次运行（无论从哪个客户端发起：curl / Yaak / Trace UI）都会被服务端记录到 `logs/traces.json`，可通过 `GET /api/v1/traces` 查询——`forge trace` 的 Trace 面板展示的就是这份全量记录，可查看所有来源的执行流程。

### 评估（Eval）

`evals/` 目录里的 Python 文件即评估套件（代码优先，同 `agents/` 模式）：每个文件导出 `suite` 或 `suites`（`EvalSuite`），启动时自动发现。运行会真实调用模型（受 suite 内 `RunPolicy` 的并发/超时/预算限制）：

```bash
curl -X POST http://127.0.0.1:8000/api/v1/evals/smoke/run      # 运行 smoke 套件
curl http://127.0.0.1:8000/api/v1/evals                        # 已发现的套件
curl http://127.0.0.1:8000/api/v1/evals/runs                   # 评估历史
curl http://127.0.0.1:8000/api/v1/traces/<run_id>/eval         # 某次运行对应的评估
```

确定性 grader（`exact / contains / regex / json_schema / tool_trajectory`）开箱即用；`llm_judge(model_factory, rubric)` 可选。结果持久化到 `logs/evals/`，与 `run_id` 关联——Trace 面板可直接看到每条 run 的得分与失败原因。

## 测试

```bash
uv run pytest                                   # 单元 + 接口（fake model，不花钱）
RUN_REAL_PROVIDER_TESTS=1 uv run pytest tests/integration   # 真实 DeepSeek（会产生费用）
```

## 修改指南

| 想做的事 | 修改位置 |
| --- | --- |
| 改 prompt / 人设 | `agents/{{ spec.module_name }}/prompts.py` |
| 加模型中间件（调用计数/上下文注入/包裹） | `agents/{{ spec.module_name }}/agent.py` 的 `middleware=[...]`；自定义实现见 SDK `ModelMiddleware` |
| 结构化输出 | 参考 `agents/extract/`：定义 pydantic schema 传给 `Agent(output_schema=...)`，结果在 `ChatResult.parsed` |
| 加 / 改工具 | `agents/{{ spec.module_name }}/tools.py`（挂到 `agent.py` 的 `tools=[...]`） |
| 新增 agent | `agents/` 下新建子目录（`agent.py` + `prompts.py` + `tools.py`），重启即自动注册 |
| 改模型 | `app/providers/llm.py` + `.env` 的 `DEEPSEEK_*` |
| 加端点 | `app/api/v1/` + `app/schemas/` |
| 改编排 / 错误码映射 | `app/services/chat_service.py` |
| 改错误码 / 响应格式 | `app/core/enums.py` + `response.py` + `handlers.py` |
| 改鉴权 | `app/core/security.py` |
| 改日志 | `app/core/logging.py` |
| 新增配置项 | `app/core/config.py` + `.env.example` |

核心约定：**一个 agent 的所有业务代码内聚在自己的目录**，框架层（app/）只提供横切能力——新增 agent 时不要动 app/ 下的代码。

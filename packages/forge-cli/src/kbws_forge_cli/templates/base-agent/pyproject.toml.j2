[project]
name = "{{ spec.name }}"
version = "0.1.0"
description = "Agent backend generated by forge ({{ spec.template }} template)"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "fastapi-cli>=0.0.5",
    "uvicorn[standard]>=0.30",
    # 等 kbws-forge-runtime 发布到 PyPI 后，取消注释即可接入运行时
    # "kbws-forge-runtime>=0.1.0",
]

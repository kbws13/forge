"""Hello World backend generated by forge."""

from fastapi import FastAPI

app = FastAPI(title="{{ spec.name }}", version="0.1.0")


@app.get("/")
def hello() -> dict[str, str]:
    return {"message": "Hello World"}
